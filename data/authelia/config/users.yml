###############################################################
#                         Users Database                      #
###############################################################

# This file can be used if you do not have an LDAP set up.

# List of users
users:
  username: # hier euren eigenen Login Namen eintragen
    displayname: "***" # hier euren eigenen Namen eintragen
    password: "Password String" # Mit diesem Befehl erzeugen: docker run -v /opt/containers/authelia/compose/config/configuration.yml:/configuration.yml -it authelia/authelia:latest authelia crypto hash generate --config /configuration.yml --password "SicheresPassword"
    email: # Eure valide Email Adresse
    groups:
      - admins
      - dev